name: Tests
on: [push]

jobs:
  test:
    name: Test Job
    runs-on: ubuntu-18.04
    steps:
      - id: setup
        name: Setup
        uses: actions/setup-node@master
      - id: install
        name: Install
        run: npm install
      - id: test
        name: Test
        env:
          API_ENDPOINT: "https://test.dt.arco.nz"
        run: npm test
